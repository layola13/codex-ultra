# codex-ultra <版本号>

> Windows x64 预览版。此 Release 只提供二进制安装包，不提供源码归档。

> GitHub 自动生成的 `Source code` 压缩包只包含产品说明仓库，不含 codex-ultra 产品源码，也不是安装包。

## 下载

| 文件 | 用途 | SHA-256 |
| --- | --- | --- |
| `codex-ultra-setup.exe` | Windows 安装版 | `<填写校验值>` |
| `codex-ultra.exe` | Windows 便携版 | `<填写校验值>` |

## 本次更新

- `<新增或改进内容>`
- `<修复内容>`
- `<兼容性或性能内容>`

## 使用前准备

- Windows 10/11 x64；
- 已安装支持 `app-server` 的 Codex CLI；
- 已准备自己的模型服务账号、API Key 和网络环境。

安装 Codex CLI：

```powershell
npm install -g @openai/codex
codex --version
```

## 已知问题

- `<填写已知问题；没有则写“暂无已知阻断问题”>`

## 升级提示

- 安装版可直接运行新安装程序覆盖升级；
- 便携版请先完全退出旧版本，再替换可执行文件；
- 重要项目与本地配置请自行备份。

问题反馈请提交 GitHub Issue，并附上版本、Windows 版本、Codex CLI 版本、复现步骤和脱敏日志。
